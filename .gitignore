# Local working assets — masters and staging for R2 uploads.
# Kept out of git so multi-MB binaries never enter history.
extras/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
